<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            style="@style/SubtitleTheme"
            android:id="@+id/hasPolicyTitle"
            android:text="@string/farmer_has_policy"/>

        <RadioGroup
            style="@style/RadioGroupTheme"
            android:id="@+id/hasPolicyGroup"
            android:orientation="horizontal"/>

        <TextView
            style="@style/SubtitleTheme"
            android:id="@+id/insuranceCompanyNameTitle"
            android:text="@string/farmer_insurance_company_name"/>

        <EditText
            style="@style/EditTextTheme"
            android:id="@+id/insuranceCompanyNameText"
            android:inputType="text" />

        <TextView
            style="@style/SubtitleTheme"
            android:id="@+id/agriculturalInformationTitle"
            android:text="@string/farmer_agricultural_information"/>

        <LinearLayout
            android:id="@+id/agriculturalInformationGroup"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:orientation="vertical"/>

    </LinearLayout>
</ScrollView>