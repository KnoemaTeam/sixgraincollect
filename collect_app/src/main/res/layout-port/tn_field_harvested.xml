<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/scrollView"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical" >

        <TextView
            style="@style/TitleTheme"
            android:text="@string/zambia_harvested" />

        <TextView
            style="@style/SubtitleTheme"
            android:id="@+id/harvestedQuantityTitle"
            android:text="@string/zambia_harvested_quantity" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:background="@android:color/transparent"
            android:focusable="true"
            android:focusableInTouchMode="true"/>

        <EditText
            style="@style/EditTextTheme"
            android:id="@+id/harvestedQuantityText"
            android:inputType="numberDecimal" />

        <TextView
            style="@style/SubtitleTheme"
            android:id="@+id/harvestedUnitTitle"
            android:text="@string/zambia_harvested_unit"/>

        <RadioGroup
            style="@style/RadioGroupTheme"
            android:id="@+id/harvestedUnitGroup"
            android:orientation="vertical" />

    </LinearLayout>
</ScrollView>