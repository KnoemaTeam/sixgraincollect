<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            style="@style/SubtitleTheme"
            android:id="@+id/isPesticideUsedTitle"
            android:text="@string/field_pesticide_used"/>

        <RadioGroup
            style="@style/RadioGroupTheme"
            android:id="@+id/isPesticideUsedGroup"
            android:orientation="horizontal"/>

        <TextView
            style="@style/SubtitleTheme"
            android:id="@+id/pesticideCostTitle"
            android:text="@string/field_pesticide_cost"/>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:background="@android:color/transparent"
            android:focusable="true"
            android:focusableInTouchMode="true"/>

        <EditText
            style="@style/EditTextTheme"
            android:id="@+id/pesticideCostText"
            android:inputType="numberDecimal"/>

    </LinearLayout>
</ScrollView>